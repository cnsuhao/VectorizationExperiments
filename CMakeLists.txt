CMAKE_MINIMUM_REQUIRED(VERSION 3.0)

PROJECT(VTKm_VECTORIZATION_EXPERIMENTS)

SET(VTKm_DIR "" CACHE FILEPATH "Path to a VTK-m build or installation")
FIND_PACKAGE(VTKm REQUIRED)

ADD_SUBDIRECTORY("tile")
